import { Redirect } from 'expo-router';

/** Provisório: o dashboard do módulo chega na Task 8 do plano 2a. */
export default function Coracao() {
  return <Redirect href="/(app)/coracao/pressao" />;
}
